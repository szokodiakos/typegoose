{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Tests",
      "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
      "args": ["--colors", "${workspaceRoot}/test/*.ts"],
      "sourceMaps": true,
      "preLaunchTask": "build"
    }
  ]
}
